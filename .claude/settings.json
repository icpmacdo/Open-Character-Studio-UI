{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [ -n \"$f\" ] || exit 0; case \"$f\" in *.py) out=$(uvx ruff check \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; };; esac; }",
            "timeout": 30,
            "statusMessage": "ruff check"
          }
        ]
      }
    ]
  }
}
